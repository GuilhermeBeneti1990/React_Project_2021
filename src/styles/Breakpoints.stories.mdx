import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Breakpoints" />

# Breakpoints

Breakpoints for responsive layouts:

- Extra Small (xs): Default
- Small (sm): >= 576px
- Medium (md): >= 768px
- Large (lg): >= 992px
- Extra Large (xl): >= 1200px
